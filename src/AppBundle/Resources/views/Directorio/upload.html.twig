{% extends '::base2.html.twig' %}

{% block body -%}
    <h1>Crear Directorio</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('principal') }}">
            Volver a principal
        </a>
    </li>
</ul>
{% endblock %}