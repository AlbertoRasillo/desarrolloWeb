
{% extends '::base1.html.twig' %}

{% block body -%}
    <h1>List Archivos y directorios</h1>

    <div id="contenedor">
    <header id="cabecera_pri">
        <nav id="menu_pri">

        </nav>
    </header>
        <section id="seccion_pri">
            <article>
     <table id="tablon">
        <tr>
            <th>Fecha</th>
            <th>Nombre Carpeta</th>
            <th>Descripción</th>
        </tr>
        <form>

        </form>
        <a href="{{ path('archivo_new')}}"><img id="anadir" src="img/anadir.png" alt="add">Añadir</a>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.id }}</td>
                <td>{{ entity.nombre }}</td>
                <td><a href="{{ path('directorio_edit', { 'id': entity.id }) }}">Editar</a></td>
            </tr>
        {% endfor %}

            <!--Fila utilizada en el caso de realizar una busqueda
            <tr>
                <td></td>
                <td></td>
                <td></td>   
            </tr>
            -->
     </table>
            </article>
        </section>
    <aside id="menu_col">
        
    </aside>
    <footer id="pie"></footer>
</div>
    {% endblock %}
