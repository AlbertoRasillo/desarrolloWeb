{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Espacioalmacenamiento list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Raiz</th>
                <th>Cuota</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('espacioalmacenamiento_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.raiz }}</td>
                <td>{{ entity.cuota }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('espacioalmacenamiento_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('espacioalmacenamiento_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('espacioalmacenamiento_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
